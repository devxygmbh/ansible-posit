# MANAGED BY ANSIBLE
{% if workbench_artifactory_enabled | bool and workbench_artifactory_username | length > 0 %}
local({
  options(
    download.file.method = "curl",
    download.file.extra = "--netrc-file /etc/R/netrc"
  )
})
{% endif %}
