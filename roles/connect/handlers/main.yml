# handlers file for connect

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart rstudio-connect service # noqa: ignore-errors
  ansible.builtin.service:
    name: rstudio-connect
    state: restarted
    enabled: true
  ignore_errors: true
